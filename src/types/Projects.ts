export type ProjectCategory = string;

export type Project = {
  img: string
  name: string
  slug: string
  tags: string[]
  stack: string[]
  categories: string[]
  release: string
  description: string
  category: string[]
};
